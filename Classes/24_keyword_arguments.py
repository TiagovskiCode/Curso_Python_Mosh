# Keyword arguments allow you to pass arguments by parameter name.
# They improve code readability and eliminate reliance on positional order.

def increment(number, by):
    return number + by


print(increment(2, 1))
